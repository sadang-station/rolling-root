# Rolling Root

## 실행

Node 버전은 `.nvmrc`에 고정되어 있습니다.

```sh
nvm use
npm install
npm run dev
```

그다음 Vite가 표시한 주소(기본 `http://localhost:5173`)를 엽니다.

## 검증

```sh
npm run lint
npm run check
npm run build
```
